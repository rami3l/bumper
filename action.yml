name: "Bumper"
description: "Generate replaced text"
inputs:
  template:
    description: "Template to be used"
    required: true
  rules:
    description: "Variables in the template"
    required: true
outputs:
  time: # id of output
    description: "The time we greeted you"
  replaced:
    description: "Generated text"
  out:
    description: "Output file"
runs:
  using: "node12"
  main: "index.js"
